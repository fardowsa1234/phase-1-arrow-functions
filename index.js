// Function expression called divide that divides two numbers
const divide = function(num1, num2) {
    return num1 / num2;
};

// Arrow function called square that takes one parameter and returns its square
const square = (num) => num * num;

// Arrow function called add that takes two parameters and returns their sum
const add = (num1, num2) => num1 + num2;
